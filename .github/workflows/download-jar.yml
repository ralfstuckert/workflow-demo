name: download jar

on:
  workflow_dispatch:
    

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    
    - uses: actions/checkout@v2
    
    - name: Set up JDK 11
      uses: actions/setup-java@v2
      with:
        java-version: '11'
        distribution: 'adopt'

    - run: cat $HOME/.m2/settings.xml
    
    - name: download jar
      run: mvn org.apache.maven.plugins:maven-dependency-plugin:2.8:copy -DoutputDirectory=. -Dartifact=com.github.ralfstuckert:workflow-demo:1.0.0:jar:plain 
      env:
        GITHUB_TOKEN: ${{ secrets.PACKAGES_PAT }}
        GITHUB_ACTOR: ${{ env.GITHUB_ACTOR }}

    - run: ls -al
      
